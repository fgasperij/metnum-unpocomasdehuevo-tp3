#include "Arquero.h"

// TODO: SIN IMPLEMENTAR
void mover(double aprox){
	double lim_inf = this->juego->dameLimInf();
	double lim_sup = this->juego->dameLimSup();
	double despl_max = this->juego->dameDesplMax();
	// Verificar no mover mas de despl_max por vez
	// No pasarse de lim_inf ni lim_sup
	// Actualizar this->posicion
}
